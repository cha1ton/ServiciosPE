<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mapa de Servicios - ServiciosPe</title>
    <link rel="stylesheet" href="dashboard.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <header class="navbar">
        <div class="logo">
            <a href="/dashboard" class="a-logo">
                <span class="logo-icon">SP</span> ServiciosPe</a>
        </div>
        <div class="user-actions">
            <a href="/register-provider" class="btn-mi-negocio"><i class="fas fa-briefcase"></i> Mi Negocio</a>
            <div class="dropdown-container">
                <a href="#" class="user-profile" id="userProfileToggle">
                    <img src="<%= user.photo || '/user_placeholder.png' %>" alt="User Avatar" class="user-avatar">
                    <span id="user-name"><%= user.name || 'Usuario' %></span>
                </a>
                <div class="dropdown-menu" id="userDropdown">
                    <a href="/profile" class="dropdown-item"><i class="fas fa-user"></i> Mi Perfil</a>
                    <a href="#" class="dropdown-item"><i class="fas fa-heart"></i> Favoritos</a>
                    <div class="dropdown-divider"></div>
                    <a href="/auth/logout" class="dropdown-item logout-item"><i class="fas fa-sign-out-alt"></i> Cerrar Sesión</a>
                </div>
            </div>
        </div>
    </header>

    <main class="content">
        <div class="map-view-container">
            <h2>Mapa Interactivo de Servicios</h2>
            <img src="images/mapa_placeholder.jpg" alt="Mapa de Lima" class="map-placeholder-image">
            <div class="map-marker-text marker-restaurant" style="top: 25%; left: 20%; background-color: var(--primary-color);">
                <i class="fas fa-utensils"></i> La Casona
            </div>
            <div class="map-marker-text marker-laundry" style="top: 45%; right: 25%; background-color: var(--secondary-color);">
                <i class="fas fa-tshirt"></i> Limpio Express
            </div>
            <div class="map-marker-text marker-health" style="bottom: 30%; left: 35%; background-color: #FFC107;">
                <i class="fas fa-heartbeat"></i> Clínica Salud Total
            </div>
            <div class="map-marker-text marker-beauty" style="top: 30%; right: 10%; background-color: #E91E63;">
                <i class="fas fa-spa"></i> Belleza Zen
            </div>

            <p style="margin-top: 20px; font-weight: 600; z-index: 1;">Arrastra el mapa y haz zoom para explorar</p>
            <p style="z-index: 1; font-size: 0.9em; color: var(--text-dark);">
                <i class="fas fa-route" style="color: #FF7043;"></i> Ruta simulada desde tu ubicación
            </p>
            <button class="btn-primary" style="margin-top: 20px; z-index: 1;" onclick="alert('Simulando navegación con Google Maps...');"><i class="fas fa-map-marked-alt"></i> Abrir en Google Maps</button>
        </div>
    </main>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const userProfileToggle = document.getElementById('userProfileToggle');
            const userDropdown = document.getElementById('userDropdown');
            if (userProfileToggle && userDropdown) {
                userProfileToggle.addEventListener('click', (e) => {
                    e.preventDefault();
                    userDropdown.classList.toggle('show');
                });
                window.addEventListener('click', (e) => {
                    if (!userProfileToggle.contains(e.target) && !userDropdown.contains(e.target)) {
                        userDropdown.classList.remove('show');
                    }
                });
            }
        });
    </script>
</body>
</html>